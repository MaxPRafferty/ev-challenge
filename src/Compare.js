import {
    VictoryBar,
    VictoryChart,
    VictoryAxis,
    VictoryTheme,
    VictoryStack,
} from "victory";

const Compare = () => {
    return <>Comparison Data</>;
};

export default Compare;
